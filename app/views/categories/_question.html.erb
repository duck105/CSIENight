<% if new_or_not(question) <= 3 %>
  <%= content_tag(:li, class: "Set") do %>
  	<%= question_check_mark(question) %>
    <%= link_to question.title, question_path(question) %>
  <% end %>
<% else %>
  <li class="Set">    
    <%= question_check_mark(question) %>
    <%= link_to question.title, question_path(question) %>
  </li>
<% end %>